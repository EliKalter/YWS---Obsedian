>The dim of the input vector representing a candidate $l$

>The dim of the output of an interviewer $l'$

>The score type $D$ ($comp([100]) \leq comp([0,1])$)

>The output type $R$ ($comp(S_k) \leq comp([100]) \leq comp([0,1])$)

>Output ties ($comp(\text{Not allowd}) \leq comp(\text{Allowd})$)

>The ammount of candidates $k$

>The ammount of allowd candidates $r$

>The ammount of interviewers $n$

>Probability ($comp(\text{Not using}) \leq comp(\text{Using})\wedge comp(D = [100]) \leq comp(D = [0,1])$)