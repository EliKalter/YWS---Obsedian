>Def (Monotonic function):
>	Let $A,B$ be sets, equiped with $\prec_A,\prec_B$ semi-connex relations on $A,B$ respectively.
>	And let $f:A\to B$.
>	We say that $f$ is monotonous/monotonic when $\forall a,a' \in A: a \prec_A a' \implies f(a) \prec_B f(a')$

