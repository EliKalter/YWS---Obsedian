>BE CAREFULL!!!!
>	When adding new functions to the following list, one must make sure it does not conflict with any existing already defined projections. (Example of the problems that can come up...) TODO elie

>There has to be no more than one from A to B, direct and indirect. TODO elie


>List (The atomic natural projections):
>
>	>$$\left([100]^k_\ne, \prescript{[k]}{}{>}, (x_1,\dots,x_k) \mapsto \left\{ (i,j) \in [k]^2 \mid x_i > x_j \right\} \right)$$
>	
>	>$$\left([0,1]^k_\ne, \prescript{[k]}{}{>}, (x_1,\dots,x_k) \mapsto \left\{ (i,j) \in [k]^2 \mid x_i > x_j \right\} \right)$$
>	
>	>$$\left(S_k, \prescript{[k]}{}{>}, \sigma \mapsto \left\{ (i,j) \in [k]^2 \mid \sigma (i) < \sigma (j) \right\}\right)$$
>	
>	>$$\left(\prescript{[k]}{}{>}, S_k, R \mapsto (i \mapsto \left\vert \left\{ j \in [k] \mid j R i \right\} \right\vert)\right)$$
>	
>	>$$\left(\prescript{[k]}{}{>}, \begin{pmatrix} [k] \\ r \end{pmatrix}, R \mapsto \left\{ i \in [k] \mid\: \left\vert \left\{ j \in [k] \mid j R i \right\} \right\vert < r \right\} \right)$$
>	
>	>$$\left( L_k, \prescript{[k]}{}{>}, Y \mapsto \left\{ (i,j) \in [k]^2 \mid i \ne j \wedge Y\left( i,j \right) = 1 \right\} \right)$$
>	>Remark: Need to change the definition of $Y$. TODO nour
>	
>	>$$\left(\prescript{[k]}{}{>}, L_k, R \mapsto ((i,j) \mapsto \begin{cases} \mathbb{1}_{\left\{ (t,s) \in [k]^2 \mid tRs \right\}} (i,j) & i < j \\ \mathbb{1}_{\left\{ (t,s) \in [k]^2 \mid tRs \right\}} (j,i) & j < i \end{cases}) \right)$$
>	>Note: I'm not a 100% sure about this HAHA

^937368